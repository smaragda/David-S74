spring:
  datasource:
    url: jdbc:h2:mem:s74_vouchers_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false
  mail:
    host: localhost
    port: 1025

s74:
  frontend-base-url: http://localhost:8000
  stripe:
    api-key: sk_test_placeholder
    webhook-secret: whsec_placeholder
  mail:
    from: vouchers@s74.test
